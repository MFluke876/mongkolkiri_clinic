
const FullScreenLoader = () => (
  <div className="min-h-screen flex items-center justify-center bg-background">
    <div className="animate-pulse-soft text-primary font-display">
      กำลังโหลด...
    </div>
  </div>
);

export default FullScreenLoader;